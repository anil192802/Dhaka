##
## NepTrade Pro — Nginx Configuration
## Place this in /etc/nginx/sites-available/neptrade
## Then: sudo ln -s /etc/nginx/sites-available/neptrade /etc/nginx/sites-enabled/
##       sudo nginx -t && sudo systemctl reload nginx
##

server {
    listen 80;
    server_name your-domain.com www.your-domain.com;

    # Redirect HTTP → HTTPS (after certbot setup)
    # return 301 https://$host$request_uri;

    location / {
        proxy_pass         http://127.0.0.1:3000;
        proxy_http_version 1.1;

        # WebSocket support
        proxy_set_header   Upgrade    $http_upgrade;
        proxy_set_header   Connection "upgrade";

        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;

        proxy_read_timeout  120s;
        proxy_send_timeout  120s;
        proxy_connect_timeout 10s;

        # Cache static assets
        location ~* \.(js|css|png|jpg|ico|woff2?)$ {
            proxy_pass http://127.0.0.1:3000;
            proxy_cache_valid 200 1d;
            add_header Cache-Control "public, max-age=86400";
        }
    }

    # Gzip
    gzip            on;
    gzip_types      text/plain text/css application/json application/javascript;
    gzip_min_length 1000;
}

## HTTPS block — uncomment after: sudo certbot --nginx -d your-domain.com
# server {
#     listen 443 ssl http2;
#     server_name your-domain.com;
#     ssl_certificate     /etc/letsencrypt/live/your-domain.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/your-domain.com/privkey.pem;
#     include /etc/letsencrypt/options-ssl-nginx.conf;
#     ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;
#     ... (same location block as above)
# }
